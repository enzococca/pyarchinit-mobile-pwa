# Production Environment Variables
# These will be set in Railway dashboard

# Database
USE_SQLITE=true
SQLITE_DB_PATH=./pyarchinit_db.sqlite

# Security
SECRET_KEY=${SECRET_KEY}
JWT_SECRET=${JWT_SECRET}
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=43200

# CORS (will be updated after frontend deployment)
ALLOWED_ORIGINS=https://your-frontend-url.vercel.app,http://localhost:5173

# Media paths (Railway ephemeral storage)
PYARCHINIT_MEDIA_ROOT=/app/media
PYARCHINIT_MEDIA_THUMB=/app/media/thumb  
PYARCHINIT_MEDIA_RESIZE=/app/media/resize

# Optional: AI Services (if needed)
# OPENAI_API_KEY=${OPENAI_API_KEY}
# ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
